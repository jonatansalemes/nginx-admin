export class Server {
    private endpoint:string;
    private alias:string;

    constructor(endpoint:string,alias:string) {
        this.endpoint = endpoint;
        this.alias = alias;
    }

    
}